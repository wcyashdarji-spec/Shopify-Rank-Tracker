[project]
name = "shopify-rank-tracker"
version = "0.1.0"
description = "A FastAPI application for tracking Shopify App Store keyword rankings."
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "fastapi",
    "uvicorn",
    "playwright",
    "pydantic",
    "sqlalchemy",
    "psycopg2-binary",
    "python-dotenv",
    "pandas",
    "openpyxl",
    "streamlit",
    "requests",
    "pyjwt",
    "pydantic-ai>=2.18.0",
    "logfire>=4.39.0",
    "pydantic-ai-slim[google,openai]>=2.18.0",
    "agentmail>=0.5.9",
    "arize-phoenix-otel>=0.17.1",
    "openinference-instrumentation-pydantic-ai>=0.1.20",
    "openinference-instrumentation-openai>=0.1.54",
    "opentelemetry-exporter-otlp>=1.44.0",
    "redis>=8.1.0",
]

[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["app*"]
exclude = [
    "logs*",
    "screenshots*",
    "shopifyvenv*",
]
